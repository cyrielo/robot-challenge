require_relative '../../app/helpers/application_helper'

RSpec.describe ToyRobot::ApplicationHelper, '#parse_commands' do
  context 'when you pass a valid command parameter' do
    it 'should parse the string and return a correct hash ' do
      expect(1).eql?(1)
    end
  end

  context 'when you pass an in-valid command parameter' do
    it 'should parse the string and return a correct hash ' do
      expect(1).eql?(1)
    end
  end

  context 'when you pass a valid command parameter' do
    it 'should parse the string and return a correct hash ' do
      expect(1).eql?(1)
    end
  end
end